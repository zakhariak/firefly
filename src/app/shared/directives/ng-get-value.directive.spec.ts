import { NgGetValueDirective } from './ng-get-value.directive';

describe('NgGetValueDirective', () => {
  it('should create an instance', () => {
    const directive = new NgGetValueDirective();
    expect(directive).toBeTruthy();
  });
});
