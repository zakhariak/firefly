import { NgBlurDirective } from './ng-blur.directive';

describe('NgBlurDirective', () => {
  it('should create an instance', () => {
    const directive = new NgBlurDirective();
    expect(directive).toBeTruthy();
  });
});
